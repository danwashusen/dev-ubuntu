---
# Copy this file to developer_extra_tasks.yml, or to a named profile such as
# developer_extra_tasks/rails.yml, and add normal Ansible tasks. Both locations
# are ignored by Git; select a named profile with DEVELOPER_EXTRA_TASKS_FILE.
#
# Example (replace the empty list below):
#
# - name: Install checkout-specific apt packages
#   ansible.builtin.apt:
#     name:
#       - package-name
#     state: present
#     update_cache: true
[]
